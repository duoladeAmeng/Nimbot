你现在要帮我完成一个代码重构 / 精简迁移任务。
我本地目前有两个项目：
- nanobot：原始项目，功能完整，但工程规模较大、实现细节较复杂，我目前正在准备秋招，没有足够时间完整阅读和理解整个项目。（在E:\CodeDir\nanobot\）
- duolaAgent：我基于 nanobot 新建的精简版项目，目前已经手动迁移了一部分代码。
我的目标不是简单复制 nanobot，也不是重新设计一个新的 Agent 框架，而是：
把 nanobot 重构成一个更小、更容易阅读、更适合学习和面试讲解的 duolaAgent，同时尽可能保留 nanobot 的核心架构思想、核心抽象和关键代码接口。
请始终遵循以下原则：
1. 保留 nanobot 的核心思想
重点保留真正体现 Agent 架构设计的部分，例如：
- Agent 主循环
- LLM 调用流程
- Message / Context 管理
- Tool 定义、注册、调用机制
- Tool Calling
- Session / Memory / History 等核心状态管理
- Agent 与工具之间的调用链路
- 配置加载
- 必要的异步调用设计
- 核心异常处理和执行流程
对于兼容性相关的复杂设计，可以大幅简化。
2. 核心 API 尽量与 nanobot 保持一致
对于 nanobot 中关键的：
- 类名
- 函数名
- 方法名
- 参数名
- 参数含义
- 核心数据结构
- 模块职责
如果没有非常必要的理由，请尽量保持一致。
例如原项目中如果存在类似：
```python
class Agent:
    ...

async def run(...):
    ...

async def execute_tool(...):
    ...

```
那么 duolaAgent 中也尽量保留这些命名。
这样做的目的，是让我在阅读 duolaAgent 后，再回头阅读 nanobot 时，可以快速建立一一对应关系。
不要为了“代码更漂亮”而随意改名。
4. 不要过度简化
虽然要精简，但不能变成一个几十行的 toy demo。
我要的是：
“nanobot 的教学版 / 面试版 / mini 版”
而不是重新写一个 Hello World Agent。
核心模块之间仍然应该具有清晰的工程结构。
例如可以保留类似：
```text
duolaAgent/
├── agent/
├── tools/
├── llm/
├── session/
├── config/
└── ...

```
具体目录结构请优先参考 nanobot 原项目，不要完全重新设计。
5. 优先复用和迁移，不要凭空重写
在实现某个模块之前：
先阅读 nanobot 对应实现，再阅读 duolaAgent 当前已有实现。
然后判断：
- 可以直接迁移哪些代码
- 哪些代码应该裁剪
- 哪些抽象必须保留
- 哪些工程逻辑可以删除
- duolaAgent 当前代码是否已经完成了一部分
- 是否需要修改已有迁移代码
不要脱离 nanobot 原实现，自己重新发明一套架构。
请先完整分析两个项目，而不是马上大面积修改代码。
第一阶段先完成代码结构分析。
请重点梳理 nanobot 的核心调用链，例如：
```text
用户输入
  ↓
Agent.run / loop
  ↓
构造 messages / context
  ↓
调用 LLM
  ↓
LLM 返回文本或 tool call
  ↓
执行 tool
  ↓
tool result 写回 messages
  ↓
再次调用 LLM
  ↓
最终输出

```
然后找出这条链路分别涉及哪些：
- 文件
- 类
- 函数
- 数据结构
并判断 duolaAgent 当前已经迁移到了什么程度。
在真正修改代码前，先给我一个简洁的迁移计划，例如：
```text
1. Agent 主循环
   nanobot:
   xxx/agent.py
   duolaAgent:
   xxx/agent.py
   状态：已部分迁移
   处理方式：保留 xxx，删除 xxx

2. Tool 系统
   ...

3. Session
   ...

4. LLM Client
   ...

```
不要写很长的项目介绍，我更关心具体源码映射。
每处理一个模块，都遵循：
找到 nanobot 中对应源码。
说明该模块在整个 Agent 系统中的作用。
明确：
- 哪些代码属于核心逻辑
- 哪些属于工程复杂度
- 哪些可以删除
优先保持：
- 类名一致
- 方法名一致
- 参数名一致
- 调用关系一致
减少不必要依赖。
确认迁移后：
```text
调用方 → 当前模块 → 下游模块

```
仍然能够正常连接。
不要只保证单文件“看起来能运行”。
duolaAgent 是给我学习源码用的，所以请适当增加注释。
但不要给每一行代码都写注释。
重点解释：
- 为什么存在这个类
- 为什么这里需要循环
- tool call 为什么要重新加入 messages
- 某个状态为什么需要保存
- 这个抽象在 nanobot 中解决什么问题
- 当前版本相比 nanobot 删除了什么复杂设计
特别重要的代码，可以写：
```python
# nanobot 中这里还处理了 xxx / xxx。
# duolaAgent 为了突出 Agent 主流程，只保留 xxx。

```
这样我以后对照源码会更容易。
不要：
- 一上来重写整个 duolaAgent
- 完全脱离 nanobot 自己设计架构
- 随意修改核心类名和方法名
- 为了追求“最佳实践”加入新的复杂抽象
- 引入重量级框架替代原本实现
- 把所有东西塞进一个文件
- 把项目简化成只能展示 demo 的玩具代码
- 删除对理解 Agent 原理有价值的代码
- 重复实现 duolaAgent 中已经迁移完成且没有明显问题的部分
如果 duolaAgent 现有实现和 nanobot 不完全一致，先判断是不是我之前为了精简而有意修改的，不要机械覆盖。
面对一段 nanobot 源码时，可以问：
“如果我要在面试中向面试官解释一个 Agent 从用户输入，到调用 LLM，到调用工具，再到返回最终答案，这段代码是否有助于解释这个过程？”
如果答案是“有”，尽量保留。
如果答案是：
“主要是为了生产环境稳定性、兼容性、扩展性或极端情况”
则可以考虑删除或简化。
最终我应该能够通过阅读 duolaAgent：
1. 在较短时间内理解 nanobot 的整体架构；
2. 理解一个完整 Agent 的执行流程；
3. 看懂 Agent、LLM、Tool、Session 等模块之间如何协作；
4. 再阅读 nanobot 时能快速找到对应源码；
5. 在秋招面试中清楚讲出项目架构和关键设计；
6. 可以实际运行，而不是只有伪代码；
7. 代码规模明显小于 nanobot，但核心思想仍然保留。
现在请先：
1. 阅读本地 nanobot 项目；
2. 阅读本地 duolaAgent 项目；
3. 判断我已经迁移了哪些模块；
4. 梳理 nanobot 最核心的 Agent 执行链路；
5. 找出 duolaAgent 当前最应该继续迁移的部分；
6. 给出一份按优先级排序的迁移计划；
7. 然后从最高优先级模块开始实际修改代码。
过程中请始终以：
“最小化代码规模，但最大化保留 nanobot 核心设计思想”
作为最高原则。
另外，每次准备删除 nanobot 中一个看起来比较重要的抽象时，请先判断：
这个抽象究竟是 Agent 原理的一部分，还是生产级工程复杂度的一部分。
对于前者保留，对于后者简化。